# Copy to .env.local (gitignored) and fill in your own values.

# Where apps/rest-api is reachable — point this at your deployed server, or
# http://localhost:3000 if you're running rest-api locally too.
NEXT_PUBLIC_API_BASE_URL=http://167.71.99.75:3000

# Pre-fills the login form's Tenant ID field — convenience only, still editable.
NEXT_PUBLIC_DEFAULT_TENANT_ID=63ceb6d3-701b-4295-8f54-18f33769628c
